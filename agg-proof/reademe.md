lib:

```
    <!-- <!-- requid galois  --> -->
    python3 -m pip install galois --break-system-packages
    
        <!-- <!-- requid py-ecc  --> -->
    python3 -m pip install py-ecc --break-system-packages
```

run:
```
cd agg-proof

python3 proof-agg.py
```